{
  "rules": {
     "bookmark": { // only allows bookmark/
        ".read": "auth != null",
        ".write": "auth != null",
        "comic": {".indexOn": ["id", "bmdb", "title", "alternative"]},
        "novel": {".indexOn": ["id", "bmdb", "title", "alternative"]},
        "anime": {".indexOn": ["id", "bmdb", "title", "alternative"]}
     }
  }
}
