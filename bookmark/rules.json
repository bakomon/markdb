{
  "rules": {
     "bookmark": { // only allows bookmark/
        ".read": "auth != null",
        ".write": "auth != null",
        "comic": {".indexOn": ["id", "title", "alternative"]},
        "novel": {".indexOn": ["id", "title", "alternative"]},
        "anime": {".indexOn": ["id", "title", "alternative"]}
     }
  }
}
